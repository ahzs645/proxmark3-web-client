import { RibbonStrip, RibbonNote } from "../primitives";

export function SettingsTab() {
  return (
    <RibbonStrip>
      <RibbonNote>
        Configure application settings, preferences, and data management in the panel below.
      </RibbonNote>
    </RibbonStrip>
  );
}

export default SettingsTab;
